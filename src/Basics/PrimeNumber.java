package Basics;

import java.util.Scanner;

//code to validate whether input entered by user is a prime number or not
public class PrimeNumber {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter number to check whether it is a leap year or not:");
        int number = sc.nextInt();
        boolean isPrime = true;
//        for(int )
    }
}
